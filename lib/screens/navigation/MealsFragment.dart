import 'package:flutter/material.dart';

class MealsFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Refeições"),
    );
  }
}